{% extends '::base.html.twig' %}
{% block pageName %}Edycja <small>problemu</small>{% endblock %}
{% block breadcrumbs %}
    {{ 
    render_breadcrumbs([
        { content: 'Lista problemów', path: 'problem'},
        { content: 'Edycja problemu'},
    ]) 
    }}
{% endblock %}
{% block content %}
    {{ form(edit_form, { 'style': 'horizontal', 'label_col': 2, 'widget_col': 10 }) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('problem') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
