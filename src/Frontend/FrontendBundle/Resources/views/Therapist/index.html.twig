{% extends 'FrontendBundle::base.html.twig' %}
{% block content %}	
    <div class="section_header">
        <div class="row"><div class="col-sm-12">
                <br>
                <h3>Zapraszamy do przeglądania filmów wg terapeuty:</h3>
                <br>
            </div>
        </div>
    </div>
    <div class="row">
        <center>
            {% for therapist in therapists %}
                <div class="col-xs-2 text-center kategorie">
                    <strong>{{ therapist.name }}</strong><br/><br/>

                        <img src="{{ asset(therapist.picture)  }}" alt="cover" class="picColor"/>

                    </br></br>
                    <a class="btn btn-default btn-sm" href="{{ path('frontend.therapist.therapist_page', { id: therapist.id }) }}">Przeglądaj</a>
                </div>
            {% endfor %}
        </center>
    </div>
{% endblock %}
{% block afterContent %}{% endblock %}